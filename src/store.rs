mod store {
    pub struct Store {
      pub x : u32,
    }

    pub fn insert(s : Store, key: String, val: String) {
    }
    pub fn lookup(s : Store, key: String, val: String) -> bool {
        return true;
    }
}
